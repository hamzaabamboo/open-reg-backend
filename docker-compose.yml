version: "3.2"

services:
  backend: 
    image: open-reg-backend
    container_name: open-reg-backend
    environment:
      MONGO_URL:
      SECRET:
      NODE_ENV:
      CHULA_APP_ID:
      CHULA_APP_SECRET:
      AWS_ACCESS_KEY_ID:
      AWS_SECRET_ACCESS_KEY:
      AWS_S3_BUCKET_NAME:
